 task-4
name: Python CI

on:
  push:
    branches:
      - main
      - task-1
      - task-2
      - task-3
      - task-4   # Added your current working branch
=======
name: CI

on:
  push:
    branches: [ main, task-3, task-4 ]
main
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
 task-4
      - uses: actions/checkout@v3
=======
      # 1. Checkout the repository
      - name: Checkout repository
        uses: actions/checkout@v3
main

      # 2. Set up Python
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
task-4
          python-version: 3.11
=======
          python-version: '3.10'
main

      # 3. Install dependencies (latest compatible versions)
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
 task-4
          pip install -r requirements.txt

      - name: Lint with flake8
        run: |
          pip install flake8
          flake8 .

          pip install pandas numpy scipy scikit-learn matplotlib seaborn flake8 xgboost shap

      # 4. Lint Python scripts only
      - name: Lint Python scripts
        run: |
          flake8 scripts/ --max-line-length=120
 main
